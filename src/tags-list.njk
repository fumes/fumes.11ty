---
templateEngineOverride: njk
eleventyExcludeFromCollections: true
layout: default
tags:
  - template {# so it can be filtered out of deployment #}
---
<h1>Tags</h1>

{% for tag in collections.tagList %}
  {% set tagUrl %}/{{ tag }}/{% endset %}
  <a href="{{ tagUrl | url }}" class="tag">{{ tag }}</a>
{% endfor %}
